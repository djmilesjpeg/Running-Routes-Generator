# Normalise to LF in the repo. The pre-commit hook is a shell script: if it
# were checked out with CRLF, bash would fail on the carriage returns.
* text=auto eol=lf

.githooks/* text eol=lf
*.sh        text eol=lf
*.mjs       text eol=lf
*.js        text eol=lf

*.png binary
*.ico binary
