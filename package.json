{
  "name": "loop-route-generator",
  "version": "1.0.0",
  "description": "Round-trip running route generator with distance correction. Static site, no build step.",
  "type": "module",
  "private": true,
  "license": "MIT",
  "scripts": {
    "start": "node scripts/dev-server.mjs",
    "dev": "node scripts/dev-server.mjs",
    "test": "node --test \"test/*.test.js\"",
    "test:watch": "node --test --watch \"test/*.test.js\"",
    "install-hooks": "git config core.hooksPath .githooks && echo Hooks installed: .githooks",
    "check": "bash .githooks/pre-commit --all",
    "icons": "node scripts/make-icons.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
